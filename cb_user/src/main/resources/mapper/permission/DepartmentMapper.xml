<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkhh.provider.mapper.permission.DepartmentMapper">
    <!--部门列表-->
    <select id="listDepartmentPageInfo" resultType="com.jkhh.provider.pojo.vo.permission.department.ListDepartmentVO">
        SELECT
        d.id,d.name,m.name as leaderName
        FROM base_department d
        LEFT JOIN base_manager m ON m.id=d.leader_id
        where d.delete_flag=0
        ORDER BY d.create_time desc
    </select>
</mapper>
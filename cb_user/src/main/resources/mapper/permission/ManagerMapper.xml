<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jkhh.provider.mapper.permission.ManagerMapper">
    <!--用户分页列表-->
    <select id="listManagerPage"
            parameterType="com.jkhh.provider.pojo.dto.permission.manager.QueryManagerListDTO"
            resultType="com.jkhh.provider.pojo.vo.permission.manager.ListManagerVO">
        select m.id,m.name,m.email,m.mobile,m.create_time createTime,d.name departmentName
        from base_manager m left join base_department d on m.department_id=d.id
        where m.delete_flag=0 and d.delete_flag=0
        <if test="name != null">
            and m.name LIKE CONCAT('%',#{name},'%')
        </if>
        <if test="departmentId != null and departmentId!=0">
            and d.id = #{departmentId}
        </if>
        order by m.create_time desc
    </select>
</mapper>
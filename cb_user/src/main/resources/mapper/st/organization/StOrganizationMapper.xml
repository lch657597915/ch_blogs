<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jkhh.provider.mapper.st.organization.StOrganizationMapper">
    <select id="listStPermissionAccount" resultType="com.jkhh.provider.pojo.vo.st.permission.ListStPermissionAccountVO">
    select a.id,h.id hospitalId,h.name hospitalName,a.principal_name as principalName,a.principal_phone as principalPhone from st_organization a left join st_permission_hospital h on a.hospital_id=h.id
    where a.delete_flag=0 and a.parent_id=#{parentId} and type=3 order by a.create_time desc
  </select>
</mapper>